# SQLite for local/dev (zero setup). For production, point this at PostgreSQL instead —
# see the note at the top of prisma/schema.prisma for how to switch the provider.
DATABASE_URL="file:./dev.db"

# Change this before deploying anywhere real.
JWT_SECRET="change-this-secret-in-production"

PORT=4000
